import { ApplyFormatsDirective } from './apply-formats.directive';

describe('ApplyFormatsDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyFormatsDirective();
    expect(directive).toBeTruthy();
  });
});
